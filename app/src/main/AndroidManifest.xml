<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.rocky_geralt.practicaempresarial">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".app.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.Registro" android:label="Registro"/>
        <activity android:name=".activities.Principal" android:label="Principal"/>
        <activity android:name=".activities.Servicios" android:label="Servicios"/>
        <activity android:name=".activities.Acerca" android:label="Acerca de NED"/>
        <activity android:name=".activities.Ayuda" android:label="Ayuda"/>
        <activity android:name=".activities.Soporte" android:label="Soporte"/>
        <activity android:name=".activities.Noticias" android:label="Quienes somos"/>
    </application>





</manifest>